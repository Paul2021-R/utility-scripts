# claude-snap-save.timer 가 1분마다 부른다. 복구가 잠금을 쥐고 있으면 30초 기다린 뒤 이번 저장을 건너뛴다.

[Unit]
Description=Claude Code session snapshot (periodic save)
After=claude-snap.service

[Service]
Type=oneshot
ExecStart=%h/.local/bin/claude-snap save
